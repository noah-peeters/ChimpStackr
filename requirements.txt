# Pure UI requirements
pyside6_essentials==6.4.1
qt-material==2.12

# Algorithm requirements
opencv-python-headless==4.6.0.66 # TODO: Test if working on windows. Otherwise revert back to "opencv-python"
numpy==1.24.0
numba==0.56.4
rawpy==0.17.3
imageio==2.22.4
pyfftw==0.13.0
MarkupSafe==2.1.1

# TODO: Check if really needed (used for image alignment)
scipy==1.9.3 # 1.8.0 gives error on mac pyinstaller build